import React from "react";
import lodash from "lodash";

const style = {
  background: "#800",
  color: "#fff",
  padding: 12,
};

const ProductCarousel = () => <div style={style}>App 1 ProductCarousel</div>;

export default ProductCarousel;
