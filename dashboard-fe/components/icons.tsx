import ArrowIcon from "@material-ui/icons/Forward";

export const UpArrow = () => (
  <ArrowIcon style={{ color: "green", transform: "rotate(-90deg)" }} />
);

export const DownArrow = () => (
  <ArrowIcon style={{ color: "blue", transform: "rotate(90deg)" }} />
);
